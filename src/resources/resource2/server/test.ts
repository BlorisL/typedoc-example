/**
 * Metodo del server per Resource2.
 * @module Resource2
 * @namespace Server
 * @category Server
 */
export function function1(param: string): string {
  return `Server method 1 called with param: ${param}`;
}
